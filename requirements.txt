# Playwright is NOT in this file (saves RAM on 512MB). Optional: requirements-playwright.txt
#
# RENDER: Build Command must NOT run "playwright install" unless you install that file first.
# Recommended: bash render-build.sh   (or: pip install --no-cache-dir -r requirements.txt)
Flask
flask-cors>=4.0.0
flask-socketio
flask-httpauth
flask-compress>=1.15
google-auth
google-api-python-client
gspread
eventlet==0.36.1
gunicorn==22.0.0
python-dotenv
xmltodict==0.14.2
requests
reportlab
httpx
psutil==5.9.8
pypdf>=4.0
beautifulsoup4
lxml
supabase>=2.5.0
